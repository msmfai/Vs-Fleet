edition = "2021"
max_width = 100
use_small_heuristics = "Default"
# Note: imports_granularity and group_imports are nightly-only.
# Uncomment when the toolchain is pinned to nightly:
# imports_granularity = "Crate"
# group_imports = "StdExternalCrate"
