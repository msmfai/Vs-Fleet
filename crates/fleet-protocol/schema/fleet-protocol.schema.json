{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "description": "Fleet wire protocol — Hub<->face events and commands plus the Session/AgentRun objects they carry.",
  "schemaVersion": 1,
  "schemas": {
    "AgentRun": {
      "$defs": {
        "AgentKind": {
          "description": "The agent flavor of a run (README §7.2 `agent_kind`).",
          "enum": [
            "claude-code",
            "codex",
            "other"
          ],
          "type": "string"
        },
        "Confidence": {
          "description": "How trustworthy a `waiting` (or any heuristic) signal is (README §7.2, §8).\n\n**Invariant 5 (confidence honesty):** a value of `High` must only ever be\nset from an authoritative signal; heuristic inference is always `Inferred`.",
          "oneOf": [
            {
              "const": "high",
              "description": "Derived from an authoritative channel (e.g. Codex `requestApproval`,\nClaude `PermissionRequest` in Use-Terminal mode).",
              "type": "string"
            },
            {
              "const": "inferred",
              "description": "Derived from a heuristic (e.g. `PreToolUse`-without-`Stop` debounce).",
              "type": "string"
            }
          ]
        },
        "State": {
          "description": "Per-run lifecycle state (README §7.3).\n\n**D9 (locked): `Done` is KEPT DISTINCT from `Idle`.** `idle` means the run is\nalive and waiting for the next prompt; `done` means the agent reported its\ntask complete. They must never be collapsed on the wire.",
          "oneOf": [
            {
              "const": "working",
              "description": "Actively producing output / running a tool.",
              "type": "string"
            },
            {
              "const": "waiting",
              "description": "Blocked on the user (approval or question). The only state that pings.",
              "type": "string"
            },
            {
              "const": "idle",
              "description": "Alive, turn finished, awaiting the next prompt.",
              "type": "string"
            },
            {
              "const": "done",
              "description": "Agent reported its task complete. Kept distinct from `Idle` (D9).",
              "type": "string"
            },
            {
              "const": "error",
              "description": "The agent raised an error.",
              "type": "string"
            },
            {
              "const": "dead",
              "description": "Process exited or the reporter is gone past the timeout (§7.3).",
              "type": "string"
            }
          ]
        },
        "Urgency": {
          "description": "Why a run/session is demanding attention (README §7.2).\n\n`null` on the wire (absence of urgency) maps to [`Urgency::None`]; it is\nserialized as the JSON string `\"null\"` is NOT used — instead the field that\nholds urgency is `Option<Urgency>` and `None` ⇒ JSON `null`. The explicit\n`None` variant here exists so callers can name \"no urgency\" without an\n`Option` wrapper when a non-optional urgency is required by a delta.",
          "oneOf": [
            {
              "const": "approval",
              "description": "Agent is asking for an approval (run a command, edit a file, …).",
              "type": "string"
            },
            {
              "const": "question",
              "description": "Agent asked the user a question.",
              "type": "string"
            },
            {
              "const": "idle-done",
              "description": "Idle/done attention tier (the quiet tier; §15.5).",
              "type": "string"
            },
            {
              "const": "null",
              "description": "Explicitly no urgency. Serializes to the string `\"null\"`.\n\nNote: the README writes `rollup_urgency: \"...|null\"`. We model the\n*absence* of urgency on optional fields as JSON `null` via `Option`, and\nalso provide this named variant so a required urgency can express \"none\"\nwithout ambiguity. Its wire token is the literal string `null`.",
              "type": "string"
            }
          ]
        }
      },
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "additionalProperties": true,
      "description": "A single agent run within a session (README §7.2).\n\nA [`Session`] has one or more of these. `schema_version` is carried on the\nobject per §7.6.",
      "properties": {
        "agent_kind": {
          "$ref": "#/$defs/AgentKind"
        },
        "confidence": {
          "$ref": "#/$defs/Confidence",
          "description": "Confidence honesty (invariant 5): `inferred` when `waiting` came from a\nheuristic, not an authoritative signal."
        },
        "cwd": {
          "type": "string"
        },
        "diff_summary": {
          "description": "v1.5+: `{files_changed, insertions, deletions}`. Opaque for now."
        },
        "last_message": {
          "description": "Short preview of the agent's output / its question.",
          "type": [
            "string",
            "null"
          ]
        },
        "native_id": {
          "description": "Durable identity anchor: Claude `session_id` / Codex `threadId` (§7.5).",
          "type": "string"
        },
        "run_id": {
          "type": "string"
        },
        "schema_version": {
          "description": "Versioning (§7.6) — present on every object.",
          "format": "uint32",
          "minimum": 0,
          "type": "integer"
        },
        "state": {
          "$ref": "#/$defs/State"
        },
        "updated_at": {
          "description": "ISO-8601.",
          "type": "string"
        },
        "urgency": {
          "anyOf": [
            {
              "$ref": "#/$defs/Urgency"
            },
            {
              "type": "null"
            }
          ],
          "description": "`null` ⇒ no urgency."
        },
        "waiting_since": {
          "description": "ISO-8601 timestamp the run entered `waiting`; `null` otherwise.",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "required": [
        "schema_version",
        "run_id",
        "agent_kind",
        "native_id",
        "cwd",
        "state",
        "confidence",
        "updated_at"
      ],
      "title": "AgentRun",
      "type": "object"
    },
    "Command": {
      "$defs": {
        "Target": {
          "description": "A target that a command addresses. `focus`/`dismiss` may address either a\nwhole session or a single run (README writes `focus(session_id|run_id)`).",
          "oneOf": [
            {
              "properties": {
                "session_id": {
                  "type": "string"
                },
                "type": {
                  "const": "session",
                  "type": "string"
                }
              },
              "required": [
                "type",
                "session_id"
              ],
              "type": "object"
            },
            {
              "properties": {
                "run_id": {
                  "type": "string"
                },
                "type": {
                  "const": "run",
                  "type": "string"
                }
              },
              "required": [
                "type",
                "run_id"
              ],
              "type": "object"
            }
          ]
        }
      },
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "description": "A command sent from a face to the Hub.\n\nInternally tagged on `command`. Like events, the envelope tolerates unknown\nfields a newer face adds; unknown *commands* are a deserialize error so the\nHub never silently ignores an instruction it cannot honor.",
      "oneOf": [
        {
          "description": "Focus the editor window for a session or run (§12.2).",
          "properties": {
            "command": {
              "const": "focus",
              "type": "string"
            },
            "schema_version": {
              "format": "uint32",
              "minimum": 0,
              "type": "integer"
            },
            "target": {
              "$ref": "#/$defs/Target"
            }
          },
          "required": [
            "command",
            "schema_version",
            "target"
          ],
          "type": "object"
        },
        {
          "description": "Mute a session's pings (state still shown) (§15.4).",
          "properties": {
            "command": {
              "const": "mute",
              "type": "string"
            },
            "schema_version": {
              "format": "uint32",
              "minimum": 0,
              "type": "integer"
            },
            "session_id": {
              "type": "string"
            }
          },
          "required": [
            "command",
            "schema_version",
            "session_id"
          ],
          "type": "object"
        },
        {
          "description": "Unmute a session.",
          "properties": {
            "command": {
              "const": "unmute",
              "type": "string"
            },
            "schema_version": {
              "format": "uint32",
              "minimum": 0,
              "type": "integer"
            },
            "session_id": {
              "type": "string"
            }
          },
          "required": [
            "command",
            "schema_version",
            "session_id"
          ],
          "type": "object"
        },
        {
          "description": "Solo a session: mute all others (§15.4).",
          "properties": {
            "command": {
              "const": "solo",
              "type": "string"
            },
            "schema_version": {
              "format": "uint32",
              "minimum": 0,
              "type": "integer"
            },
            "session_id": {
              "type": "string"
            }
          },
          "required": [
            "command",
            "schema_version",
            "session_id"
          ],
          "type": "object"
        },
        {
          "description": "Dismiss a (typically `dead`/stale) session or run (§7.3, §15.2).",
          "properties": {
            "command": {
              "const": "dismiss",
              "type": "string"
            },
            "schema_version": {
              "format": "uint32",
              "minimum": 0,
              "type": "integer"
            },
            "target": {
              "$ref": "#/$defs/Target"
            }
          },
          "required": [
            "command",
            "schema_version",
            "target"
          ],
          "type": "object"
        },
        {
          "description": "Replace the tag set on a session.",
          "properties": {
            "command": {
              "const": "set_tags",
              "type": "string"
            },
            "schema_version": {
              "format": "uint32",
              "minimum": 0,
              "type": "integer"
            },
            "session_id": {
              "type": "string"
            },
            "tags": {
              "items": {
                "type": "string"
              },
              "type": "array"
            }
          },
          "required": [
            "command",
            "schema_version",
            "session_id",
            "tags"
          ],
          "type": "object"
        }
      ],
      "title": "Command"
    },
    "Event": {
      "$defs": {
        "AgentKind": {
          "description": "The agent flavor of a run (README §7.2 `agent_kind`).",
          "enum": [
            "claude-code",
            "codex",
            "other"
          ],
          "type": "string"
        },
        "AgentRun": {
          "additionalProperties": true,
          "description": "A single agent run within a session (README §7.2).\n\nA [`Session`] has one or more of these. `schema_version` is carried on the\nobject per §7.6.",
          "properties": {
            "agent_kind": {
              "$ref": "#/$defs/AgentKind"
            },
            "confidence": {
              "$ref": "#/$defs/Confidence",
              "description": "Confidence honesty (invariant 5): `inferred` when `waiting` came from a\nheuristic, not an authoritative signal."
            },
            "cwd": {
              "type": "string"
            },
            "diff_summary": {
              "description": "v1.5+: `{files_changed, insertions, deletions}`. Opaque for now."
            },
            "last_message": {
              "description": "Short preview of the agent's output / its question.",
              "type": [
                "string",
                "null"
              ]
            },
            "native_id": {
              "description": "Durable identity anchor: Claude `session_id` / Codex `threadId` (§7.5).",
              "type": "string"
            },
            "run_id": {
              "type": "string"
            },
            "schema_version": {
              "description": "Versioning (§7.6) — present on every object.",
              "format": "uint32",
              "minimum": 0,
              "type": "integer"
            },
            "state": {
              "$ref": "#/$defs/State"
            },
            "updated_at": {
              "description": "ISO-8601.",
              "type": "string"
            },
            "urgency": {
              "anyOf": [
                {
                  "$ref": "#/$defs/Urgency"
                },
                {
                  "type": "null"
                }
              ],
              "description": "`null` ⇒ no urgency."
            },
            "waiting_since": {
              "description": "ISO-8601 timestamp the run entered `waiting`; `null` otherwise.",
              "type": [
                "string",
                "null"
              ]
            }
          },
          "required": [
            "schema_version",
            "run_id",
            "agent_kind",
            "native_id",
            "cwd",
            "state",
            "confidence",
            "updated_at"
          ],
          "type": "object"
        },
        "Confidence": {
          "description": "How trustworthy a `waiting` (or any heuristic) signal is (README §7.2, §8).\n\n**Invariant 5 (confidence honesty):** a value of `High` must only ever be\nset from an authoritative signal; heuristic inference is always `Inferred`.",
          "oneOf": [
            {
              "const": "high",
              "description": "Derived from an authoritative channel (e.g. Codex `requestApproval`,\nClaude `PermissionRequest` in Use-Terminal mode).",
              "type": "string"
            },
            {
              "const": "inferred",
              "description": "Derived from a heuristic (e.g. `PreToolUse`-without-`Stop` debounce).",
              "type": "string"
            }
          ]
        },
        "Editor": {
          "additionalProperties": true,
          "description": "README §7.1 `editor` object. The whole object is optional on a session\n(`editor: null` is legal); within it, `kind` may itself be `null`.",
          "properties": {
            "focus_hint": {
              "description": "CLI args / URI used to focus this window.",
              "type": [
                "string",
                "null"
              ]
            },
            "kind": {
              "anyOf": [
                {
                  "$ref": "#/$defs/EditorKind"
                },
                {
                  "type": "null"
                }
              ],
              "description": "`null` when the editor flavor is unknown (README writes `...|null`)."
            }
          },
          "type": "object"
        },
        "EditorKind": {
          "description": "The editor flavor bound to a session (README §7.1 `editor.kind`).",
          "enum": [
            "vscode",
            "cursor",
            "windsurf"
          ],
          "type": "string"
        },
        "Location": {
          "additionalProperties": true,
          "description": "README §7.1 `location` object.",
          "properties": {
            "attach_hint": {
              "description": "Hint for attaching to a remote/docker locale; `null` for local.",
              "type": [
                "string",
                "null"
              ]
            },
            "glyph": {
              "$ref": "#/$defs/LocationGlyph"
            },
            "kind": {
              "$ref": "#/$defs/LocationKind"
            },
            "label": {
              "type": "string"
            }
          },
          "required": [
            "kind",
            "label",
            "glyph"
          ],
          "type": "object"
        },
        "LocationGlyph": {
          "description": "Glyph hint for the location (README §7.1).",
          "enum": [
            "laptop",
            "docker",
            "remote"
          ],
          "type": "string"
        },
        "LocationKind": {
          "description": "Where the session physically lives (README §7.1 `location`).",
          "enum": [
            "local",
            "docker",
            "remote"
          ],
          "type": "string"
        },
        "Server": {
          "additionalProperties": true,
          "description": "README §7.1 `server` object.",
          "properties": {
            "kind": {
              "$ref": "#/$defs/ServerKind"
            },
            "version": {
              "type": [
                "string",
                "null"
              ]
            }
          },
          "required": [
            "kind"
          ],
          "type": "object"
        },
        "ServerKind": {
          "description": "The server backing the session (README §7.1 `server.kind`).",
          "enum": [
            "code-server",
            "openvscode-server",
            "desktop-remote",
            "local"
          ],
          "type": "string"
        },
        "Session": {
          "additionalProperties": true,
          "description": "A tracked VS Code-Server environment (README §7.1).\n\n`rollup_state` / `rollup_urgency` are the most-urgent state/urgency across\nthe session's runs (computed by the Hub merge engine; see [`crate::rollup`]).",
          "properties": {
            "editor": {
              "anyOf": [
                {
                  "$ref": "#/$defs/Editor"
                },
                {
                  "type": "null"
                }
              ],
              "description": "`null` when no editor is bound."
            },
            "location": {
              "$ref": "#/$defs/Location"
            },
            "muted": {
              "default": false,
              "type": "boolean"
            },
            "policy": {
              "description": "Opaque policy blob (§17, not modeled in v1)."
            },
            "rollup_state": {
              "$ref": "#/$defs/State",
              "description": "Worst/most-urgent state across `runs`."
            },
            "rollup_urgency": {
              "anyOf": [
                {
                  "$ref": "#/$defs/Urgency"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Most-urgent urgency across `runs`; `null` ⇒ none."
            },
            "runs": {
              "items": {
                "$ref": "#/$defs/AgentRun"
              },
              "type": "array"
            },
            "schema_version": {
              "description": "Versioning (§7.6) — present on every object.",
              "format": "uint32",
              "minimum": 0,
              "type": "integer"
            },
            "server": {
              "$ref": "#/$defs/Server"
            },
            "session_id": {
              "description": "Stable durable session id.",
              "type": "string"
            },
            "soloed": {
              "default": false,
              "type": "boolean"
            },
            "tags": {
              "default": [],
              "items": {
                "type": "string"
              },
              "type": "array"
            },
            "title": {
              "type": "string"
            },
            "unread": {
              "default": false,
              "type": "boolean"
            },
            "updated_at": {
              "description": "ISO-8601.",
              "type": "string"
            }
          },
          "required": [
            "schema_version",
            "session_id",
            "title",
            "location",
            "server",
            "runs",
            "rollup_state",
            "updated_at"
          ],
          "type": "object"
        },
        "State": {
          "description": "Per-run lifecycle state (README §7.3).\n\n**D9 (locked): `Done` is KEPT DISTINCT from `Idle`.** `idle` means the run is\nalive and waiting for the next prompt; `done` means the agent reported its\ntask complete. They must never be collapsed on the wire.",
          "oneOf": [
            {
              "const": "working",
              "description": "Actively producing output / running a tool.",
              "type": "string"
            },
            {
              "const": "waiting",
              "description": "Blocked on the user (approval or question). The only state that pings.",
              "type": "string"
            },
            {
              "const": "idle",
              "description": "Alive, turn finished, awaiting the next prompt.",
              "type": "string"
            },
            {
              "const": "done",
              "description": "Agent reported its task complete. Kept distinct from `Idle` (D9).",
              "type": "string"
            },
            {
              "const": "error",
              "description": "The agent raised an error.",
              "type": "string"
            },
            {
              "const": "dead",
              "description": "Process exited or the reporter is gone past the timeout (§7.3).",
              "type": "string"
            }
          ]
        },
        "Urgency": {
          "description": "Why a run/session is demanding attention (README §7.2).\n\n`null` on the wire (absence of urgency) maps to [`Urgency::None`]; it is\nserialized as the JSON string `\"null\"` is NOT used — instead the field that\nholds urgency is `Option<Urgency>` and `None` ⇒ JSON `null`. The explicit\n`None` variant here exists so callers can name \"no urgency\" without an\n`Option` wrapper when a non-optional urgency is required by a delta.",
          "oneOf": [
            {
              "const": "approval",
              "description": "Agent is asking for an approval (run a command, edit a file, …).",
              "type": "string"
            },
            {
              "const": "question",
              "description": "Agent asked the user a question.",
              "type": "string"
            },
            {
              "const": "idle-done",
              "description": "Idle/done attention tier (the quiet tier; §15.5).",
              "type": "string"
            },
            {
              "const": "null",
              "description": "Explicitly no urgency. Serializes to the string `\"null\"`.\n\nNote: the README writes `rollup_urgency: \"...|null\"`. We model the\n*absence* of urgency on optional fields as JSON `null` via `Option`, and\nalso provide this named variant so a required urgency can express \"none\"\nwithout ambiguity. Its wire token is the literal string `null`.",
              "type": "string"
            }
          ]
        }
      },
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "description": "An event pushed from the Hub to a face.\n\nThe `type` tag uses the README's dotted names. `#[serde(tag = \"type\")]`\nmakes them internally tagged so the discriminator and payload share one\nJSON object. We do NOT `deny_unknown_fields` here: faces must tolerate\nenvelope fields a newer Hub adds (invariant 2). Unknown variants, however,\nare a hard deserialize error by design — a face that sees an event `type`\nit cannot interpret should surface that rather than silently drop state.",
      "oneOf": [
        {
          "description": "Full list of sessions, sent on subscribe (README §7.4).",
          "properties": {
            "schema_version": {
              "format": "uint32",
              "minimum": 0,
              "type": "integer"
            },
            "sessions": {
              "items": {
                "$ref": "#/$defs/Session"
              },
              "type": "array"
            },
            "type": {
              "const": "fleet.snapshot",
              "type": "string"
            }
          },
          "required": [
            "type",
            "schema_version",
            "sessions"
          ],
          "type": "object"
        },
        {
          "description": "A session newly appeared.",
          "properties": {
            "schema_version": {
              "format": "uint32",
              "minimum": 0,
              "type": "integer"
            },
            "session": {
              "$ref": "#/$defs/Session"
            },
            "type": {
              "const": "session.added",
              "type": "string"
            }
          },
          "required": [
            "type",
            "schema_version",
            "session"
          ],
          "type": "object"
        },
        {
          "description": "A session changed (full object acceptable per §7.4).",
          "properties": {
            "schema_version": {
              "format": "uint32",
              "minimum": 0,
              "type": "integer"
            },
            "session": {
              "$ref": "#/$defs/Session"
            },
            "type": {
              "const": "session.updated",
              "type": "string"
            }
          },
          "required": [
            "type",
            "schema_version",
            "session"
          ],
          "type": "object"
        },
        {
          "description": "A session was removed; only its id is needed.",
          "properties": {
            "schema_version": {
              "format": "uint32",
              "minimum": 0,
              "type": "integer"
            },
            "session_id": {
              "type": "string"
            },
            "type": {
              "const": "session.removed",
              "type": "string"
            }
          },
          "required": [
            "type",
            "schema_version",
            "session_id"
          ],
          "type": "object"
        },
        {
          "description": "A run newly appeared within a session.",
          "properties": {
            "run": {
              "$ref": "#/$defs/AgentRun"
            },
            "schema_version": {
              "format": "uint32",
              "minimum": 0,
              "type": "integer"
            },
            "session_id": {
              "type": "string"
            },
            "type": {
              "const": "run.added",
              "type": "string"
            }
          },
          "required": [
            "type",
            "schema_version",
            "session_id",
            "run"
          ],
          "type": "object"
        },
        {
          "description": "A run changed.",
          "properties": {
            "run": {
              "$ref": "#/$defs/AgentRun"
            },
            "schema_version": {
              "format": "uint32",
              "minimum": 0,
              "type": "integer"
            },
            "session_id": {
              "type": "string"
            },
            "type": {
              "const": "run.updated",
              "type": "string"
            }
          },
          "required": [
            "type",
            "schema_version",
            "session_id",
            "run"
          ],
          "type": "object"
        },
        {
          "description": "A run was removed; ids only.",
          "properties": {
            "run_id": {
              "type": "string"
            },
            "schema_version": {
              "format": "uint32",
              "minimum": 0,
              "type": "integer"
            },
            "session_id": {
              "type": "string"
            },
            "type": {
              "const": "run.removed",
              "type": "string"
            }
          },
          "required": [
            "type",
            "schema_version",
            "session_id",
            "run_id"
          ],
          "type": "object"
        }
      ],
      "title": "Event"
    },
    "Session": {
      "$defs": {
        "AgentKind": {
          "description": "The agent flavor of a run (README §7.2 `agent_kind`).",
          "enum": [
            "claude-code",
            "codex",
            "other"
          ],
          "type": "string"
        },
        "AgentRun": {
          "additionalProperties": true,
          "description": "A single agent run within a session (README §7.2).\n\nA [`Session`] has one or more of these. `schema_version` is carried on the\nobject per §7.6.",
          "properties": {
            "agent_kind": {
              "$ref": "#/$defs/AgentKind"
            },
            "confidence": {
              "$ref": "#/$defs/Confidence",
              "description": "Confidence honesty (invariant 5): `inferred` when `waiting` came from a\nheuristic, not an authoritative signal."
            },
            "cwd": {
              "type": "string"
            },
            "diff_summary": {
              "description": "v1.5+: `{files_changed, insertions, deletions}`. Opaque for now."
            },
            "last_message": {
              "description": "Short preview of the agent's output / its question.",
              "type": [
                "string",
                "null"
              ]
            },
            "native_id": {
              "description": "Durable identity anchor: Claude `session_id` / Codex `threadId` (§7.5).",
              "type": "string"
            },
            "run_id": {
              "type": "string"
            },
            "schema_version": {
              "description": "Versioning (§7.6) — present on every object.",
              "format": "uint32",
              "minimum": 0,
              "type": "integer"
            },
            "state": {
              "$ref": "#/$defs/State"
            },
            "updated_at": {
              "description": "ISO-8601.",
              "type": "string"
            },
            "urgency": {
              "anyOf": [
                {
                  "$ref": "#/$defs/Urgency"
                },
                {
                  "type": "null"
                }
              ],
              "description": "`null` ⇒ no urgency."
            },
            "waiting_since": {
              "description": "ISO-8601 timestamp the run entered `waiting`; `null` otherwise.",
              "type": [
                "string",
                "null"
              ]
            }
          },
          "required": [
            "schema_version",
            "run_id",
            "agent_kind",
            "native_id",
            "cwd",
            "state",
            "confidence",
            "updated_at"
          ],
          "type": "object"
        },
        "Confidence": {
          "description": "How trustworthy a `waiting` (or any heuristic) signal is (README §7.2, §8).\n\n**Invariant 5 (confidence honesty):** a value of `High` must only ever be\nset from an authoritative signal; heuristic inference is always `Inferred`.",
          "oneOf": [
            {
              "const": "high",
              "description": "Derived from an authoritative channel (e.g. Codex `requestApproval`,\nClaude `PermissionRequest` in Use-Terminal mode).",
              "type": "string"
            },
            {
              "const": "inferred",
              "description": "Derived from a heuristic (e.g. `PreToolUse`-without-`Stop` debounce).",
              "type": "string"
            }
          ]
        },
        "Editor": {
          "additionalProperties": true,
          "description": "README §7.1 `editor` object. The whole object is optional on a session\n(`editor: null` is legal); within it, `kind` may itself be `null`.",
          "properties": {
            "focus_hint": {
              "description": "CLI args / URI used to focus this window.",
              "type": [
                "string",
                "null"
              ]
            },
            "kind": {
              "anyOf": [
                {
                  "$ref": "#/$defs/EditorKind"
                },
                {
                  "type": "null"
                }
              ],
              "description": "`null` when the editor flavor is unknown (README writes `...|null`)."
            }
          },
          "type": "object"
        },
        "EditorKind": {
          "description": "The editor flavor bound to a session (README §7.1 `editor.kind`).",
          "enum": [
            "vscode",
            "cursor",
            "windsurf"
          ],
          "type": "string"
        },
        "Location": {
          "additionalProperties": true,
          "description": "README §7.1 `location` object.",
          "properties": {
            "attach_hint": {
              "description": "Hint for attaching to a remote/docker locale; `null` for local.",
              "type": [
                "string",
                "null"
              ]
            },
            "glyph": {
              "$ref": "#/$defs/LocationGlyph"
            },
            "kind": {
              "$ref": "#/$defs/LocationKind"
            },
            "label": {
              "type": "string"
            }
          },
          "required": [
            "kind",
            "label",
            "glyph"
          ],
          "type": "object"
        },
        "LocationGlyph": {
          "description": "Glyph hint for the location (README §7.1).",
          "enum": [
            "laptop",
            "docker",
            "remote"
          ],
          "type": "string"
        },
        "LocationKind": {
          "description": "Where the session physically lives (README §7.1 `location`).",
          "enum": [
            "local",
            "docker",
            "remote"
          ],
          "type": "string"
        },
        "Server": {
          "additionalProperties": true,
          "description": "README §7.1 `server` object.",
          "properties": {
            "kind": {
              "$ref": "#/$defs/ServerKind"
            },
            "version": {
              "type": [
                "string",
                "null"
              ]
            }
          },
          "required": [
            "kind"
          ],
          "type": "object"
        },
        "ServerKind": {
          "description": "The server backing the session (README §7.1 `server.kind`).",
          "enum": [
            "code-server",
            "openvscode-server",
            "desktop-remote",
            "local"
          ],
          "type": "string"
        },
        "State": {
          "description": "Per-run lifecycle state (README §7.3).\n\n**D9 (locked): `Done` is KEPT DISTINCT from `Idle`.** `idle` means the run is\nalive and waiting for the next prompt; `done` means the agent reported its\ntask complete. They must never be collapsed on the wire.",
          "oneOf": [
            {
              "const": "working",
              "description": "Actively producing output / running a tool.",
              "type": "string"
            },
            {
              "const": "waiting",
              "description": "Blocked on the user (approval or question). The only state that pings.",
              "type": "string"
            },
            {
              "const": "idle",
              "description": "Alive, turn finished, awaiting the next prompt.",
              "type": "string"
            },
            {
              "const": "done",
              "description": "Agent reported its task complete. Kept distinct from `Idle` (D9).",
              "type": "string"
            },
            {
              "const": "error",
              "description": "The agent raised an error.",
              "type": "string"
            },
            {
              "const": "dead",
              "description": "Process exited or the reporter is gone past the timeout (§7.3).",
              "type": "string"
            }
          ]
        },
        "Urgency": {
          "description": "Why a run/session is demanding attention (README §7.2).\n\n`null` on the wire (absence of urgency) maps to [`Urgency::None`]; it is\nserialized as the JSON string `\"null\"` is NOT used — instead the field that\nholds urgency is `Option<Urgency>` and `None` ⇒ JSON `null`. The explicit\n`None` variant here exists so callers can name \"no urgency\" without an\n`Option` wrapper when a non-optional urgency is required by a delta.",
          "oneOf": [
            {
              "const": "approval",
              "description": "Agent is asking for an approval (run a command, edit a file, …).",
              "type": "string"
            },
            {
              "const": "question",
              "description": "Agent asked the user a question.",
              "type": "string"
            },
            {
              "const": "idle-done",
              "description": "Idle/done attention tier (the quiet tier; §15.5).",
              "type": "string"
            },
            {
              "const": "null",
              "description": "Explicitly no urgency. Serializes to the string `\"null\"`.\n\nNote: the README writes `rollup_urgency: \"...|null\"`. We model the\n*absence* of urgency on optional fields as JSON `null` via `Option`, and\nalso provide this named variant so a required urgency can express \"none\"\nwithout ambiguity. Its wire token is the literal string `null`.",
              "type": "string"
            }
          ]
        }
      },
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "additionalProperties": true,
      "description": "A tracked VS Code-Server environment (README §7.1).\n\n`rollup_state` / `rollup_urgency` are the most-urgent state/urgency across\nthe session's runs (computed by the Hub merge engine; see [`crate::rollup`]).",
      "properties": {
        "editor": {
          "anyOf": [
            {
              "$ref": "#/$defs/Editor"
            },
            {
              "type": "null"
            }
          ],
          "description": "`null` when no editor is bound."
        },
        "location": {
          "$ref": "#/$defs/Location"
        },
        "muted": {
          "default": false,
          "type": "boolean"
        },
        "policy": {
          "description": "Opaque policy blob (§17, not modeled in v1)."
        },
        "rollup_state": {
          "$ref": "#/$defs/State",
          "description": "Worst/most-urgent state across `runs`."
        },
        "rollup_urgency": {
          "anyOf": [
            {
              "$ref": "#/$defs/Urgency"
            },
            {
              "type": "null"
            }
          ],
          "description": "Most-urgent urgency across `runs`; `null` ⇒ none."
        },
        "runs": {
          "items": {
            "$ref": "#/$defs/AgentRun"
          },
          "type": "array"
        },
        "schema_version": {
          "description": "Versioning (§7.6) — present on every object.",
          "format": "uint32",
          "minimum": 0,
          "type": "integer"
        },
        "server": {
          "$ref": "#/$defs/Server"
        },
        "session_id": {
          "description": "Stable durable session id.",
          "type": "string"
        },
        "soloed": {
          "default": false,
          "type": "boolean"
        },
        "tags": {
          "default": [],
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "title": {
          "type": "string"
        },
        "unread": {
          "default": false,
          "type": "boolean"
        },
        "updated_at": {
          "description": "ISO-8601.",
          "type": "string"
        }
      },
      "required": [
        "schema_version",
        "session_id",
        "title",
        "location",
        "server",
        "runs",
        "rollup_state",
        "updated_at"
      ],
      "title": "Session",
      "type": "object"
    }
  },
  "title": "fleet-protocol"
}
