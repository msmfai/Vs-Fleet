{
  "name": "fleet-host-ui",
  "version": "0.1.0",
  "private": true,
  "description": "Fleet host Tauri rail frontend — tests + static IPC/webview checks.",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^4.1.9",
    "jsdom": "^25.0.1",
    "vitest": "^4.1.9"
  }
}
