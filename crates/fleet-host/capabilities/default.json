{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Fleet IPC for the rail webview only (the editor surface is a plain external origin with no Fleet API access).",
  "webviews": ["rail"],
  "permissions": ["core:default"]
}
